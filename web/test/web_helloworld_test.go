package web_test

import (
	"testing"
)

// 可以测试一次请求
// 但不要在这里启动服务
// 应该在 main 方法中启动服务
func TestWebHelloWorld(t *testing.T) {
	//http.HandleFunc("/", web.SayHelloName)       // 设置访问的路由
	//err := http.ListenAndServe(":9090", nil) // 设置监听的端口
	//if err != nil {
	//	log.Fatal("ListenAndServe: ", err)
	//}
}



